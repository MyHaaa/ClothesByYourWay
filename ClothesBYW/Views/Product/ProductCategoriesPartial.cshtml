@model List<Models.EF.ProductCategory>
@using Models.ViewModels.Customer.Product

@foreach (var item in Model)
{
    @Html.ActionLink(item.Name, "Index", new ProductListRequest(item.ProductCategoryID), new { @class = "nav-link" })
}